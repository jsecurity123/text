fullScreen();
String title = "cue cards study Tools";
PFont titleFont;
titleFont = createFont( "AgencyFB-Reg", 100);
rect(displayWidth*1/4, displayHeight*0, displayWidth*1/2, displayHeight*1/8);
fill(0); //Black ink
textAlign (CENTER);
textFont( titleFont,100); 
text(title,displayWidth*1/4, displayHeight*0, displayWidth*1/2, displayHeight*1/8);
textSize(displayHeight*1/8);
Float textLength =displayWidth*1/2);
for(Float = 1; textWidth(title)> displayWidth*1/2;) {
  textLength = Length*i;
  textSize(textLength);
}
fill(255); // defualt ink, white